const tokens = {
  discord: "string",
  pg: "string",
};

const config = {
  prefix: "string",
};

const validators = { config, tokens };

export { config, tokens, validators };
export default validators;
